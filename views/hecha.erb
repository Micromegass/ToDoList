
    <div class="container text-center">
        <div class="form-group">
        <div class="card">
                <div class="card-header">
                 <h4> Tareas Hechas <h4>
                </div>
                
                <div class="card-body">
                     

                        <% @tareas.each do |tarea| %>
                        <% if tarea["done"] == true %>
                        <ul class="form-group">
                                <%= tarea["title"] %>


                        </ul>

                          <div class=form-group>
                                <form action="/tarea/<%=tarea["id"]%>" method="post">
                                <input class="btn btn-danger" type="hidden" name="_method" value="DELETE">
                                <input type="submit" value="Borrar?">
                                </form>
                                </div>
                        <% end  %>
                         <% end  %>


                </div>
                 <p><a href="/"> regresar a tareas</a></p>

                </div class="card-footer">
                         © All rights reserved
                </div>
        </div>
        </div>
        </div>